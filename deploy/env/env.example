# Example env files for `deploy/compose.yaml` and generated `compose-yowking.yaml`
#
# Copy/paste the relevant sections into separate files:
# - deploy/env/nats.env
# - deploy/env/yowapi.env
# - deploy/env/king.env
# - deploy/env/kingyaml.env (currently optional / can be empty)
#
# Notes:
# - `compose.yaml` uses `env_file` for container env vars.
# - The NATS service command also references `${NATS_TOKEN}` for Compose interpolation.
#   Export `NATS_TOKEN` in your shell (or create a top-level `deploy/.env`) before `docker compose up`.

###############################################################################
# deploy/env/nats.env
###############################################################################
# Used by: nats service command interpolation (and can be shared with services)
NATS_TOKEN=replace-with-long-random-token

###############################################################################
# deploy/env/yowapi.env
###############################################################################
# Used by: yowapi
NATS_TOKEN=replace-with-same-nats-token
NATS_URL=nats
JWT_KEY=replace-with-long-random-jwt-signing-key
# ADMIN_USER=your_lichess_username

# Needed if yowapi should mirror finished games to Lichess.
LICHESS_TOKEN=replace-with-lichess-api-token

###############################################################################
# deploy/env/king.env
###############################################################################
# Used by: yowking containers generated by deploy/buildKingYaml.mjs
NATS_TOKEN=replace-with-same-nats-token-as-above
NATS_URL=nats

# Present in your real file; only some are used by kingworker directly.
# JWT_KEY=replace-with-jwt-key
# PORT=8443
# LICHESS_TOKEN=replace-with-lichess-api-token

# Optional for local WSL runs only; not typically needed in Linux Docker deploys.
# IS_WSL=true

###############################################################################
# deploy/env/kingyaml.env
###############################################################################
# Not currently referenced by the checked-in deploy scripts. Safe to leave empty.
